#!/bin/bash

SESSION=gmt-promap

tmux new-session -d -s $SESSION

tmux new-window -n ssh-promap-prod ssh gmt-promap-prod
tmux new-window -n ssh-promap-dev ssh gmt-promap-dev
tmux new-window -n psql-promap-prod psql-gmt-promap-prod
tmux new-window -n psql-promap-dev psql-gmt-promap-dev

